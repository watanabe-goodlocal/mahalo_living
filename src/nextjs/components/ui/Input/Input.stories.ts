import Input from "./Input";

const inputStory = {
  title: "ui/Input",
  component: Input,
  args: {},
};

export default inputStory;

export const Primary = {
  args: {
    label: "お名前",
    required: true,
    placeholder: "例）山田　太郎",
  },
};
