import Input from "./Checkbox";

const inputStory = {
  title: "ui/Input",
  component: Input,
  args: {},
};

export default inputStory;

export const Primary = {
  args: {
    label: "プライバシーポリシーに同意する",
    required: true,
  },
};
